return {
    "github/copilot.vim",
    config = function()
        -- Copilot is automatically configured
        -- You can add custom copilot keymaps here if needed
        -- vim.g.copilot_no_tab_map = true
        -- vim.keymap.set('i', '<C-J>', 'copilot#Accept("\\<CR>")', { expr = true, replace_keycodes = false })
    end,
}